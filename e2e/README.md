# End-to-End Tests

- `web/`: Playwright tests for critical user journeys, responsive behavior, and visual regression.
- `mobile/`: Appium-based Android smoke/e2e harness.

Run web tests with:

`npm run test:e2e:web`

Run mobile tests with:

`npm run test:e2e:mobile`
